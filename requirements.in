# fusion-cowork v0.4.0 依赖源 (uv pip compile -> requirements.lock)
# 主依赖 + [cloud] + [web] + [test] (生产+开发测试一并锁, pip install -r 即全)
pydantic>=2.0.0
aiofiles>=23.0.0
click>=8.1.0
rich>=13.0.0
watchdog>=3.0.0
apscheduler>=3.10.0
httpx>=0.27.0
aiosqlite>=0.20.0
pyyaml>=6.0
jinja2>=3.1.0
# [web]
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
# [cloud]
pyjwt>=2.8
asyncpg>=0.29
cryptography>=42
prometheus-client>=0.20
opentelemetry-api>=1.24
opentelemetry-sdk>=1.24
structlog>=24
# fusion-core / fusion-identity: in-tree monorepo packages (NOT on PyPI).
# Installed via `pip install -e fusion-core` / `-e fusion-identity` from the
# monorepo root, NOT via this project's extras. Imports guarded lazily
# (is_identity_enabled() / try-except) so absence is a no-op, never an import error.
# [test]
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
pytest-mock>=3.14.0
